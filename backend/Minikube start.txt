Minikube start

kubectl create deployment mynginx --image=nginx

Kubectl get deployments

Kubectl expose deployment mynginx --type = NodePort

--port=80 --target-port=80

kubectl scale deployment myngix --replicas=4

kubectl get deployments

kubectl get pods

kubectl port-forward svc/mynginx 8081:80


new
minikube dashboard



to clean up

kübeckl detele deployment mynginx

kubectl delete service myngix

minikube stop






docker username:phaniharika1
docker info
docker pull jasonrivers/nagios:latest
docker run -d --name nagios -p 9090:80 jasonrivers/nagios:latest
Default login:

User: nagiosadmin

Password: nagios




Step 1: Pull the Nagios Docker Image
•	Open a terminal and run:
docker pull jasonrivers/nagios:latest
Step 2: Run Nagios
•	Command:
docker run --name nagiosdemo -p 8888:80 jasonrivers/nagios:latest
Step 3: Access Nagios Dashboard
•	Open your browser and navigate to:
http://localhost:8888
o	Login Credentials:
	Username: nagiosadmin
	Password: nagios
o	Once logged in, explore:
	Hosts: View systems being monitored.
	Services: Check tasks being monitored (e.g., CPU usage).
	Alerts: Access recent notifications.
Step 4: Monitoring Host Details
1.	Navigate to the Host Information Page:
o	Select a host from the Hosts menu.
2.	Key Details:
o	Host Status: Indicates if the system is UP or DOWN.
o	Metrics: View CPU usage, memory status, and network activity.
o	Actions: Reschedule checks, disable notifications, or schedule downtime.
Step 5: Stop and Remove Nagios
1.	Stop the Container:
o	Command:
docker stop nagiosdemo
2.	Delete the Container:
o	Command:
docker rm nagiosdemo
3.	Remove the Image (Optional):
o	List images:
docker images
o	Delete the Nagios image:
docker rmijasonrivers/nagios:latest







1) Jenkins:
TOPICS: 2 pipeline, 3 pipeline, scripted pipeline, webhooks, email config
PREREQUISITES:
• in GitHub prepare with maven java project,
• Maven Webprojects,
• ngrok configuration,
• configuring your email.

2) minikube (kubernetes):
PREREQUISITES:
• have image in docker hub (try to push one object in docker hub),
• Docker daemon should be running

3) nagios:
PREREQUISITES: Docker should be running

4) aws:
PREREQUISITES: Maven-web in github

5) uml: ---------------> definite qn ***
TOPICS: usecase diagram, class diagram, sequence diagram, component diagram
EXAMPLE: ATM machine, online ticket reservation system, book banking

NOTE: 4 to 5 questions are given in exam. UML is compulsory question.





pom.xml
src/
webapp/


git clone <YOUR_MAVEN_PROJECT_HTTPS_URL>

FROM maven:3.8.5-openjdk-11 AS build
WORKDIR /app
COPY . .
RUN mvn clean package

FROM tomcat:9
COPY target/*.war /usr/local/tomcat/webapps/


sudo docker run -d -p 9090:9090 mymavenapp



